1. 문제 페이지에 들어가자 마자 페스워드를 입력하라는 팝업창이 떴다.
2. 아무값도 입력하지 않고 확인을 눌렀더니 "wrong password"라는 팝업창이 떴다.
3. 페이지 소스를 보니까 javascript 코드에 pass값과 함께 로그인 방법이 나와있었다.
4. pass값은 hex인코딩 되어있는 것 같고, 문제를 해결하기 위해서 window.prompt()와 unescape()함수를 알아야 될 것 같았다.
5. 두 함수의 용도를 찾아본 후 pass 값을 hex디코딩 하여 문제를 해결하였다.


===
- window.prompt(): 사용자가 텍스트를 입력할 수 있도록 안내하는 선택적 메시지를 가지고 있는 대화 상자를 띄움
- unescape(): escape를 통해 만들어진 uri 이스케이핑을 디코드/ 인코딩된 문자열을 디코딩
- hex 인코딩 문자열: %문자와 숫자로 이루어져 있음