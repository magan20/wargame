- nosql(not only sql): 문서 저장소, 키값 저장소, 그래프 등과 같은 다른 저장 메커니즘에 의존하는 비관계형 데이터베이스임. 
1. get 방식으로 login과 pass 파라미터가 전송됨
2. $ne를 이용해 admin으로 로그인 메커니즘 우회 시 사용자, test로 로그인 되었음
3. $ne를 이용해 test으로 로그인 메커니즘 우회 시 admin으로 로그인 되었음
4. admin도 test도 아닌 사용자를 찾는 쿼리를 전송 했을 때 flag가 나왔음


{}[]() : 관계 묶음
'," : 문자열 처리
: : 연산자
$ne : not equal
$exist : 키 존재 여부
$where : 필터링 스크립트
$regex : 정규 표현식
$gt: >
$lt: <